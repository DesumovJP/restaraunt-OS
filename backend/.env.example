# ==================================================
# RESTAURANT OS - BACKEND ENVIRONMENT VARIABLES
# ==================================================
# Copy this file to .env and fill in the values

# Server
HOST=0.0.0.0
PORT=1337
NODE_ENV=development

# Security keys (generate each with: openssl rand -base64 32)
APP_KEYS="key1,key2,key3,key4"
API_TOKEN_SALT=your-api-token-salt
ADMIN_JWT_SECRET=your-admin-jwt-secret
TRANSFER_TOKEN_SALT=your-transfer-token-salt
JWT_SECRET=your-jwt-secret

# ==================================================
# DATABASE
# ==================================================

# Local development (SQLite)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Production (PostgreSQL on Railway)
# Railway provides DATABASE_URL automatically when you add PostgreSQL
# DATABASE_CLIENT=postgres
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# ==================================================
# FILE UPLOADS - DigitalOcean Spaces
# ==================================================
# Required for image uploads in production
# Create a Space at: https://cloud.digitalocean.com/spaces

# DO_SPACE_ACCESS_KEY=your-access-key
# DO_SPACE_SECRET_KEY=your-secret-key
# DO_SPACE_ENDPOINT=fra1.digitaloceanspaces.com
# DO_SPACE_REGION=fra1
# DO_SPACE_BUCKET=restaurant-os-uploads
# DO_SPACE_CDN=https://restaurant-os-uploads.fra1.cdn.digitaloceanspaces.com

# ==================================================
# CORS - Frontend URL
# ==================================================
# Comma-separated list of allowed origins
FRONTEND_URL=http://localhost:3000
# Production: FRONTEND_URL=https://your-app.vercel.app

# ==================================================
# RAILWAY PRODUCTION VARIABLES
# ==================================================
# Set these in Railway Dashboard -> Variables:
#
# NODE_ENV=production
# DATABASE_CLIENT=postgres
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# APP_KEYS=(generate 4 base64 keys, comma-separated)
# API_TOKEN_SALT=(generate base64 key)
# ADMIN_JWT_SECRET=(generate base64 key)
# TRANSFER_TOKEN_SALT=(generate base64 key)
# JWT_SECRET=(generate base64 key)
# FRONTEND_URL=https://your-app.vercel.app
# DO_SPACE_ACCESS_KEY=...
# DO_SPACE_SECRET_KEY=...
# DO_SPACE_ENDPOINT=fra1.digitaloceanspaces.com
# DO_SPACE_REGION=fra1
# DO_SPACE_BUCKET=restaurant-os-uploads
